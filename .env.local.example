# Dodo Payments API (server-side)
DODO_BEARER_TOKEN=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook secret for signature verification (server-side)
DODO_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxx

# Where the hosted checkout redirects after success/failure
# We recommend redirecting to /success to allow time for webhook processing.
DEFAULT_RETURN_URL=http://localhost:3000/success

# Product IDs (exposed to client at build-time)
NEXT_PUBLIC_DODO_PRODUCT_MONTHLY=pdt_xxxxxxxxx_monthly
NEXT_PUBLIC_DODO_PRODUCT_YEARLY=pdt_xxxxxxxxx_yearly